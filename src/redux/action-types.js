// Action types for settings reducer
export const UPDATE_SETTINGS = 'Update settings data in redux';
export const CLEAR_SETTINGS = 'Clear settings data in redux';

// Action types for settings epics
export const FETCH_SETTINGS = 'Fetch settings data from server';

// Action types for builds data
export const FETCH_BUILD_HISTORY = 'Fetch build histories from server';
export const UPDATE_BUILD_HISTORY = 'Update build histories in redux';
export const CLEAR_BUILD_HISTORY = 'Clear build histories from redux';
